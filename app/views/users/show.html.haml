= render 'layouts/navbar'
%body.home-body

.container
	-if current_user == nil || current_user.id != @user.id
	-else
		%h1 mysong.is/#{@user.username}
		-if current_user.picture == nil
			%h3.salt #{@user.username}'s Preferred music: #{@user.permalink}
		-else
			%h3.salt #{current_user.name}'s Preferred music: #{@user.permalink}

.row
	.col.s6
		-if current_user == nil || current_user.id != @user.id
		-else
		%div
			%br
			.center
				- if current_user.picture == nil
				- else
					%img.center.borderr.z-depth-3{alt: "profile_pic", src: @img}
			= react_component 'TopSongs'
	.col.s6.m4.l3
		- if params[:mixtape_id]
			= react_component 'PlayMixtape', { current_user: current_user, mixtape_id: params[:mixtape_id] }
			%img{:alt => "My QR code", :src => @qr}
		- else
			= react_component 'PlayMixtape', { current_user: current_user, mixtape_id: @mixtape_id }
			.center
				%img{:alt => "My QR code", :src => @qr}
				%br
				%br
				%br
				%br
				%br
	.col.s6
	%br
	%br
	%br
	%br
	%br
	%br
	%br
	%br
	%br


	/ %button.btn= link_to "Edit Profile", users_edit_path, class: "white-text"
	%br
	%br
	%br
	%br
	%br
	.sam= react_component 'Player'
	%br
	%br
	%br
	%br
	%br

	%link{:href => "https://fonts.googleapis.com/icon?family=Material+Icons", :rel => "stylesheet"}/

